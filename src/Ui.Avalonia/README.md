# Ui.Avalonia

Avalonia MVVM UI.

- Views (.axaml) + ViewModels (no UI framework deps)
- Use commands, not event handlers; no async void
- UI uses Application interfaces only; no direct Infrastructure
- Explicit UI thread usage when needed
